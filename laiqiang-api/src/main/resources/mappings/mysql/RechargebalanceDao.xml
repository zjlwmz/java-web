<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.emay.laiqiang.dao.RechargebalanceDao">
	
	
	<select id="findList" resultType="Rechargebalance" parameterType="hashmap">
		select * from rechargebalance t where t.memberid=#{memberid} limit #{start},#{end}
	</select>
	
</mapper>